name: PCSO Results History Scraper

on:
  schedule:
    - cron: '15 14 * * *'   # 10:15 PM PH (after the 9PM/EZ2 9PM draws settle)
  workflow_dispatch:

jobs:
  scrape-history:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Run PCSO history scraper
        run: python3 .github/scripts/scrape_pcso_history.py

      - name: Commit results
        run: |
          git config user.name "PCSO History Bot"
          git config user.email "pcso-history-bot@jomerpb.github.io"
          git add pcso-history.json
          git diff --staged --quiet || git commit -m "chore: update PCSO history $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
